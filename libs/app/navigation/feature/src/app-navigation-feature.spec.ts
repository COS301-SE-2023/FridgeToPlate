import { appNavigationFeature } from './app-navigation-feature';

describe('appNavigationFeature', () => {
  it('should work', () => {
    expect(appNavigationFeature()).toEqual('app-navigation-feature');
  });
});
